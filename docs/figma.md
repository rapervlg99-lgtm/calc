# Figma

Пока file key / node ids не заданы.

Когда появится макет:

```powershell
$env:FIGMA_TOKEN="<token>"
$env:FIGMA_FILE_KEY="<key>"
$env:FIGMA_START_NODE_ID="<node>"
```

Заполнить `docs/figma_assets.json` и экспортировать ассеты в `frontend/public/figma/`.
