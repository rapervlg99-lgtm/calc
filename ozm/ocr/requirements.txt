# --- core (обязательно) ---
PyMuPDF>=1.24            # рендер, векторная геометрия, текстовый слой, встроенные шрифты
fonttools>=4.50          # чтение glyph order исходных шрифтов -> ремонт битой кодировки
opencv-python>=4.9       # морфология, connected components, fallback-детекция сетки
numpy>=1.26
pandas>=2.2              # DataFrame-представление таблиц
openpyxl>=3.1            # экспорт XLSX
Pillow>=10.2             # рендер шаблонов глифов из TTF

# --- опционально: растровый OCR-fallback для сканов ---
# Ставить оба: режим --ocr hybrid читает наименования Tesseract'ом, а числа и
# обозначения RapidOCR — по отдельности каждый проваливает свою половину таблицы.
# rapidocr-onnxruntime>=1.3   # CPU, ONNX, без облака. ВНИМАНИЕ: базовая модель НЕ знает кириллицу
# pytesseract>=0.3.10         # требует внешний бинарь Tesseract + tessdata rus
