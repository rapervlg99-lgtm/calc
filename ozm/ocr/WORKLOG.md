# WORKLOG — ocr (распознавание пдф)

Журнал для монитора проектов. Дописывается по ходу работы.
Заголовки «Открытые вопросы» и «Журнал» не удалять — их читает монитор.

## Открытые вопросы

- коллега тестирует веб-интерфейс по туннелю; как закончит — гасим сторож и локальный сервер @2026-08-20

## Журнал

## 2026-09-11
- Вторая задача из плана 2026-09-10 — «дешёвые» оптимизации скорости без изменения результата. Сделано три из четырёх: (1) кэш шаблонов глифов `_TemplateCache` вместо `lru_cache(160)` — ограничен объёмом (`OCRPDF_TEMPLATE_CACHE_MB`, 512 по умолчанию: полный набор листа АП — 932 набора, 366 МБ) и дублируется на диск в плоском формате `.tpl` (`OCRPDF_TEMPLATE_CACHE_DIR`, предел `OCRPDF_TEMPLATE_DISK_MB` 512, в compose — том `ocr-templates`); первый вариант на `np.savez` пришлось выбросить — zip со 140 членами грузился медленнее рендера (17 мс против 8 на набор), плоский файл — 10–12 мс против 8–20; (2) `_match_score` считает все девять сдвигов двумя операциями numpy через `as_strided` (`_shift_counts`) вместо цикла со срезами — 1,4 млн вызовов на тяжёлом листе; (3) координаты векторных штрихов переводятся в систему страницы одним матричным умножением в float32 (`map_drawing_arrays`, повторяет `fz_transform_point` до бита) — `_collect_primitives` и `extract_rulings` стали двухпроходными с сохранением порядка примитивов; (4) потоки RapidOCR проверены замером на хосте и в контейнере: onnxruntime по умолчанию уже берёт все 4 ядра (5,1–5,7 с против 7,1–7,6 с на одном потоке), менять нечего
- Проверка: 203 теста (новый `tests/test_speedups.py`, 21 тест — каждая оптимизация сравнивается со старой реализацией до бита на случайных масках, реальных шаблонах и синтетических страницах с /Rotate 0/90/270). Регрессия по девяти листам на хосте: все `table_*.json` совпали поячеечно, в `processing_report.json` отличаются только время и пути; суммарно 446 с → 343 с (АП 57 → 40, Estakada 96 → 67, file-7 81 → 61, file-4 71 → 56, file-14 53 → 41). Холодный старт свежего процесса на АП: 54 с без дискового кэша, 48 с с ним. Контейнер `ocr` пересобран: АП через API — 78 с в свежем контейнере (вчера 124–127), file-7 — 70 с, после `docker restart` с прогретым томом шаблонов АП — 47 с; таблицы и арифметика те же (60/60, 8 спорных; 121/0/1, 23 спорных). Документация: README («Кэш шаблонов», абзац в «Сколько это занимает»), оба compose, `docs/deploy.md`
- Олег: «добавить кнопку „Автоматическая группировка“, которая распределяет распознанные конструкции по группам по столбцу „Конструкции“ (Фермы, Балки, Колонны…)». Сделано во фронте ОЗМ, форма ОГЗ (`frontend/src/components/ProfileRowsTable.vue`, блок «Группа элементов»): кнопка рядом с «Создать группу», логика в `frontend/src/utils/autoGroup.ts` (чистая функция, 5 тестов). Строки раскладываются по группам с названиями из колонки «Элемент конструкции» (значения канонические, из `ELEMENT_CANON` ocrpdf); группа с таким же названием (без учёта регистра/пробелов, «Колонны / Стойки» = «Колонны/Стойки») переиспользуется вместе с её R и типом конструкции, и они применяются к пришедшим строкам; строки без элемента остаются где были; опустевшие группы удаляются, заранее созданные пустые — нет; повторное нажатие ничего не меняет. Под кнопкой — итог («Распределено 4 позиции по 3 группам; новых групп: 3»), кнопка неактивна, если ни у одной строки нет элемента. Группы уходят в калькулятор прежним путём (`getGroupsForPrefill`). Проверка: vitest 52 passed, vue-tsc чистый, контейнер `frontend` пересобран, на демо-данных /recognize получились группы «Балки» (2), «Ригели» (1), «Колонны/Стойки» (1), ошибок в консоли нет
- Олег: «перед распознаванием нужно подтверждение — кнопка „Распознать“, чтобы можно было заменить ошибочно загруженный файл». Сделано на странице OCR (`ocrpdf/webapp.py`): выбор файла (перетаскивание или кнопка) больше не запускает задание, а показывает блок «Выбран файл: имя (размер)» с кнопками «Распознать» и «Заменить файл»; текст зоны загрузки меняется на «Чтобы заменить файл, перетащите другой…», повторный выбор просто заменяет ожидающий файл, не-PDF отклоняется подсказкой без сброса выбранного; настройки (страницы, DPI, OCR) можно править до старта. По «Распознать» блок скрывается и запускается прежний конвейер с очередью и остановкой. Подсказка на странице /recognize калькулятора и README обновлены. Проверено в браузере на пересобранном контейнере: выбор → замена → не-PDF → «Распознать» → задание ушло, результат показан
- Олег: «почему в file-13 не все позиции распознаются корректно?» Разбор: числа все верны; единственная ошибка чтения — поз. 2 «I20Ш1» → «2011» (буква Ш чертёжным шрифтом читается как «l1»); красная строка «Всего профиля» поз. 3 — ошибка на самом чертеже (8,9 + 12,3 ≠ 21,4, по позициям 12,48); оранжевые ячейки — верный текст с низкой уверенностью растрового OCR (шрифт листа не из референсных, шаблоны глифов дали 89% спорных на шапке, конвейер ушёл в OCR); системный пробел — подшапка «Колонны, Балки, Связи…» не входила в шапку, массы шли в безымянные колонки col4…col8, в калькуляторе «Элемент конструкции» пустой
- Сделано по разбору (пункты 1 и 2): (1) `detect_header_rows` без служебной строки номеров присоединяет к шапке подстроки многоуровневой шапки (`_extend_header_fallback`: строку накрывает row_span ячейки шапки сверху, в ней есть собственный нечисловой текст и нет чисел; не больше четырёх строк) — на file-13 колонки масс получили имена «Колонны», «Балки», «Связи вертикальные», «Связи горизонтальные», «Прогоны», на file-37 тоже («Колонны», «Балки и прогоны», «Связи, распорки», «Фермы», «Подкрановые балки» — последнее канонизировано из «Подкрановые конструкции» словарём `ELEMENT_CANON`), спорных ячеек там 34 → 24; остальные семь листов поячеечно идентичны (в file-3 в служебной generic-таблице строка ушла в шапку, на импорт не влияет); (2) марки двутавра с потерянной буквой серии («2011», «301») получают `candidates` в ячейке (`Cell.candidates`, `fix_profile_series`: Б/Ш/К по ГОСТ 26020), экспорт и API отдают `profile_size_candidates`; фронт (`ocrImport.resolveMassPerMeter`) проверяет варианты по справочнику: единственный найденный подставляется со статусом «Требует проверки», несколько или ни одного — в таблице ОГЗ под маркой появляются кнопки-варианты с массой 1 м, клик подставляет марку, массу и длину (`ProfileDataRow` → `pick-mark` → patch). Тесты: OCR 209 passed (новый `tests/test_header_sublevel.py`, 6 тестов), фронт 57 passed (новый `ocrImport.candidates.test.ts`, 5 тестов), vue-tsc чистый. Проверено в контейнерах на file-13: шапка [0, 1], элементы у строк «колонны/балка/прогон/связи вертикальные», у поз. 2 варианты 20Б1 21,3 / 20Ш1 30,6 / 20К1 41,4 кг/м, выбор 20Ш1 подставил марку и массу
- Олег: «почему для уголка 150 в file-13 не нашлась удельная масса?» Причина в словарях: марка «150х150х10» прочитана верно, но серии 150 по ГОСТ 8509-93 не было ни в справочнике масс (`backend/internal/ogz/sortament/mass_handbook.json`, копия в `backend/dicts/`), ни в справочнике проката (`backend/dicts/roll.json` — собран `dcextract` из `testdata/dc/roll.xml`, где серии 150 тоже нет). Соседний «180х180х15» (поз. 15) — профиля с такой толщиной в ГОСТ 8509-93 нет, плюс пустая графа наименования на чертеже, категория «уголок» строке не досталась
- Сделано: (1) серия 150 добавлена в оба словаря — масса 1 м по таблице ГОСТ 8509-93 (150х10 23,02; 150х12 27,39; 150х15 33,82; 150х18 40,11 кг/м), в roll.json записи corner_e с id 100029–100032 (продолжение ручных добавлений 100000+, R=16, r=5,3); копия справочника в `dicts/` синхронизирована; (2) фронт, импорт из OCR (`ocrImport.ts`): если уголка нет в обоих словарях, масса 1 м считается по размерам из марки (`angleMassPerMeterFromMark`: t·(2b−t) / t·(B+b−t), без закруглений — занижение ~1 %, строка идёт «Требует проверки»); (3) пустая графа наименования у строки данных наследует группу и ГОСТ строки выше до ближайшей строки-итога (в спецификациях группу пишут на первую позицию), с пометкой «Требует проверки» и заметкой импорта. Тесты: фронт 60 passed (новый `ocrImport.angles.test.ts`), Go `sortament`/`calc` ok. Контейнеры backend и frontend пересобраны, file-13 в браузере: поз. 14 «150х150х10» → 23,02 кг/м по справочнику, «Посчитано»; поз. 15 «180х180х15» → уголок ГОСТ 8509-93, 40,62 кг/м по формуле, «Требует проверки»
- Олег: «оформить OCR в стиле калькулятора ОЗМ, „выберите файл“ и „Распознать“ — красным вместо синего». Страница `ocrpdf/webapp.py` переведена на палитру `frontend/src/styles/tn/tokens.css`: акцент red-60 #e11b11 (hover red-65, pressed red-70, мягкий фон red-10), фон neutral-15, текст neutral-100, вторичный neutral-60, линии neutral-25; кнопки «Распознать» и «Передать в калькулятор» — как `.btn-primary` (высота 40, радиус 12, 16/600), «Заменить файл» и «Остановить» — как `.btn-ghost`; панели радиус 16, карточки 12; шрифт Proxima Nova первым в стеке (грузится только если установлен — страница внешних ресурсов не тянет). Тёмная тема оставлена с более светлым красным. Контейнер `ocr` пересобран, проверено во фрейме /recognize и на результате задания
- Регрессия по девяти листам после правок словарей и импорта (`D:\dev\ocrwork\reg_final`): все таблицы поячеечно идентичны прогону после правок шапки; разброс времени (310 → 405 с) — шум от Яндекс.Диска и контейнеров, повтор Estakada и file-7 отдельно дал 73 и 57 с
- Олег: «текст про массу по размерам сечения зелёный, почему?» Заметки импорта из OCR уходили в общее уведомление формы ОГЗ, у которого только два вида — зелёный «успех» и красный «ошибка», и затирались следующим «Строка обновлена». Теперь (`OgzFormView.vue`) они показываются отдельным жёлтым блоком «Что проверить после распознавания» над таблицей, списком по одной заметке на строку, с кнопкой «Скрыть»; уведомления оставлены для действий пользователя. Проверено на file-13: четыре заметки в блоке, зелёного сообщения нет
- Олег: «откуда в калькуляторе площадь ОЗБ по file-13?» Из строк листового проката (поз. 10–12, t 10/16/20, 3,34 т): импорт считает для них площадь (масса ÷ масса м² листа, ~27 м²), предзаполнение суммирует её в `sheet.areaM2`, а калькулятор включал по ней тумблер ОЗБ и подставлял как площадь бетона. Фасонки и накладки — металл, а не бетон; связка была подстановкой без описания в документации. Исправлено (`CalculatorView.vue`, `prefillMapper.ts`): ОЗБ из предзаполнения больше не включается, площадь листовой стали показывается справочно в заметке предзаполнения («… в расчёт не включена: калькулятор считает огнезащиту по элементам профильного проката»); поле маппера переименовано `betonAreaM2` → `sheetAreaM2`. Контракт API не менялся
- Олег: «предупреждение о строках на проверку уточнить по вкладкам и указывать номера строк». Сделано (`stores/job.ts` — `attentionRows`, `OgzFormView.vue`): предупреждение перечисляет отдельно «Профильный погонаж: N строк — № …» и «Листовая сталь: N строк — № …», название вкладки — ссылка, переключающая на неё. Номера — из колонки «№» соответствующей вкладки; в таблице погонажа «№» теперь порядковый номер в общем перечне, а не внутри окна группы (`ProfileRowsTable.rowNo`), иначе после перетаскивания в группу номер менялся и ссылка из предупреждения не совпадала. Проверено на file-13: погонаж № 3, 7; листовая сталь без замечаний; после автогруппировки номера не меняются
- Олег: «объединить блоки „Что проверить после распознавания“ и „Требуют проверки…“». Объединены в один жёлтый блок «Что проверить после распознавания» (`OgzFormView.vue`): сверху живой список строк по вкладкам с номерами («Требуют проверки или ввода массы — проверь перед подтверждением: Профильный погонаж: 2 строки — № 3, 7»), ниже раздел «Почему» с заметками импорта мелким текстом и кнопкой «Скрыть», которая убирает только причины — список строк остаётся, пока строки не исправлены. Серый блок «без предела ОС» оставлен отдельно: другое действие и другой момент (перед отправкой в калькулятор). Проверено на file-13
- Олег: «актуализировать calclab с репозитория и опубликовать ОЗМ+OCR на сайте скрытой страницей, доступной только по ссылке». Клон `D:\dev\calclab.pro` подтянут до `8a70e1f` (43 коммита; репозиторий на GitHub, не GitLab). Публикация по образцу скрытого `/cad/`: фронт ОЗМ научился жить под префиксом (`VITE_BASE_PATH` → vite `base`, роутер `createWebHistory(BASE_URL)`, `APP_BASE` в `utils/api.ts` для `/api/v1` и адреса OCR-фрейма; корень `/` по умолчанию не изменился), в calclab добавлены `deploy/ozm/compose.yaml` (prebuilt-образы `:calclab`, порт 127.0.0.1:18090, лимиты под 2 ГБ, кэш шаблонов OCR 128 МБ), `deploy/ozm/nginx.conf.template` (SPA под /ozm/, `/ozm/api/` с проверкой API_TOKEN, `/ozm/ocr/` с `X-Forwarded-Prefix /ozm/ocr`), сниппет `ozm-proxy.conf`, `install-ozm.sh`/`enable-ozm-proxy.sh`, `deploy/OZM.ru.md`, `robots.txt`; в ozm-new — `dev/scripts/build-calclab-images.ps1` (собирает три образа, `ozm.env` с токеном, `fonts.tar`, `ozm-images.tar` в `D:\dev\ozm-release`). До сервера не дошло: SSH с этой машины заблокирован политикой агента, ключ деплоя `calclab_deploy` из инструкций здесь отсутствует; нужно решение по замене старого автономного `/ocr` (порт 8090, OOM при 1,7 ГБ) на OCR нового стека и по паролю на вход
- Попутно: путь scratchpad этой среды длиннее 260 символов, из-за чего Windows не открывала файлы с длинными именами (ложные `ModuleNotFoundError`, `WinError 206`) — рабочие данные регрессии перенесены в `D:\dev\ocrwork` (снимок старого кода `base_src`, выходы `reg_base`/`reg_cur`, копии листов `ap/f7/f14.pdf`). На хосте заметка «tesseract: языки: eng+rus» в отчёте не появляется из-за кириллицы в пути к tessdata (`get_languages` молча падает) — и вчера было так же, качество не затронуто, в контейнере заметка есть

## 2026-09-10
- Олег: «OCR читает АП 2502-19-РД-КМ1-2 больше пяти минут, в чём проблема?» Разобрались: (1) лист и раньше был самым медленным в наборе — 230 с на хосте; профиль показал, что 3 минуты из 4 уходят на рендер шаблонов глифов: каждый символ рисовался на холст (3·кегль + 2·отступ)², при кегле 100–240 px это до мегапиксела на символ, а калибровка перебирает 14 кеглей на гарнитуру и кластер высот — за лист ~76 тыс. таких рендеров; вдобавок обрезанная маска шаблона оставалась срезом большого холста, и кэш шаблонов держал гигабайты пустых пикселей (процесс OCR в контейнере вырос до 4,6 ГБ + 1,8 ГБ свопа); (2) файл был загружен дважды, и два конвейера работали параллельно в одном процессе — VM Docker (6 ГБ на 13 контейнеров: dev-стек, тестовый prod-стек ozmprod, чужой flat_calc) ушла в своп (load average 32), второе задание завершилось через 38 минут, первое зависло со статусом running навсегда
- сделано: шаблоны рендерятся на тесном холсте по `getbbox` символа с копией маски (`_render`; маски, lsb и bot побитово те же — есть тест сравнения со старым способом); маски пробных контуров считаются один раз на набор, а не на каждый кегль (`_probe_masks`); веб-сервер выполняет задания строго по одному (`_run_gate`, второе ждёт в очереди со статусом running, отмена работает); задания, оставшиеся running после перезапуска процесса, при старте помечаются ошибкой «прервано перезапуском сервера». Итог на хосте: АП 2502-19 — 77 с вместо 230 при тех же таблицах, арифметике 60/60 и 10 спорных ячейках. Тесты: 176 passed (новый `tests/test_templates_fast.py`). Контейнер `ocr` пересобран: тот же лист через API — 127 с, 60/60, 8 спорных; зависшее задание после перезапуска помечено ошибкой. Регрессия по девяти листам: таблицы, спорные ячейки и арифметика идентичны прежним, суммарное время 1059 с → 496 с (file-4 235 → 72, file-7 202 → 94, Estakada 147 → 109, file-45 23 → 9)
- Олег спросил про скорость после публикации на сайте и про смысл оптимизаций на чужих таблицах. Договорились: сначала защита сервиса от тяжёлых документов, потом дешёвые оптимизации, параллелизацию — по факту нагрузки. Сделана первая задача: (1) лимит страниц на задание (`OCRPDF_MAX_PAGES`, по умолчанию 10; длиннее — ответ 413 с подсказкой указать страницы в поле «Страницы»), число страниц считается при загрузке, битый PDF отклоняется сразу; (2) лимит времени (`OCRPDF_MAX_SEC_PER_PAGE` 300 с × страницы, не меньше `OCRPDF_MIN_JOB_SEC` 600 с) через тот же кооперативный флаг, что и отмена — задание получает `error` «превышен лимит времени» с советом; (3) очередь видна: `/api/job` отдаёт `phase` и `queue_ahead`, интерфейс пишет «В очереди: перед вами N заданий», в списке недавних — «в очереди»; подсказка «20–40 с на страницу» заменена честной «от 10 с до 2–3 минут». Тесты: 182 passed (новый `tests/test_limits.py`). Переменные вынесены в оба compose-файла, deploy.md обновлён. Проверено на пересобранном контейнере: 12-страничный PDF — 413 с подсказкой, с полем «1-2» — принят; битый файл — 400; два задания подряд — у второго `phase: queued, queue_ahead: 1`, в списке недавних «в очереди», оба досчитались (60/60 и 237/237). Docker Desktop к этому моменту был выключен целиком — запущен заново
- Docker Desktop снова висел (API 500, com.docker.build с 10:23) — перезапущен; попытка остановить тестовый стек ozmprod из этой сессии не разрешена, он по-прежнему занимает память VM вместе с flat_calc

## 2026-09-09
- Олег показал `file-14 (486).pdf` — «очень плохо прочитан». Разобрались: спецификация на листе вставлена AutoCAD'ом растровой картинкой (JPEG 3168×5404, ≈400 dpi, ISOCPEUR Italic), векторных глифов и текстового слоя в таблице нет, читал только гибридный OCR, и результат был generic 46×10 без ролей: «Мm020» вместо «Итого», «(245-4», «JОТ» вместо «30Б1», «1752.» вместо «17,52», 39 спорных ячеек
- причины оказались в склейке движков, а не в них самих (RapidOCR читал числа и обозначения верно): (1) пробник первых пяти строк читал только Tesseract, служебная строка номеров «1 2 3 … 10» приходила как «ий 2 3 4 2 6 … I», шапка не находилась — теперь движок пробника выбирается по содержимому (`ENGINE_PROBE = "auto"`), короткое слово без цифр тоже за Tesseract; (2) рамка слова RapidOCR выступала за линовку на доли пункта и отбрасывалась строгим `_inside`, а рамка Tesseract по той же строке оставалась — ячейка получала «OHO» вместо «0,340»; теперь слово принадлежит ячейке, если лежит в ней целиком ИЛИ большей частью
- курсив: наклон письма оценивается по направлениям штрихов (гистограмма углов градиента, вертикальная линовка исключается; `_estimate_slant`, ±1° на синтетике, 16–17° на реальных полосах), и при наклоне от 7° полосы строк, повёрнутые подписи шапки и штамп читаются Tesseract дважды — как есть и выпрямленными сдвигом; для каждой строки берётся вариант с большей уверенностью. «Y20/KU CMO/bHbIE» → «Уголки стальные горячекатаные неравнополочные», «Байки» → «Балки», «ndDHOO» → «Прогоны». Высокая ячейка без слов горизонтального OCR теперь сразу пробуется с поворотом (раньше оставалась пустой)
- роли: если «№ п.п.» не прочиталась и роли считаются от размера профиля, колонка сразу за размером без словесной подписи — это «№ п.п.» (раньше номера позиций уходили в массы по элементам, таблица не становилась спецификацией); шапка блока масс узнаётся нечётко («по зиементам»)
- после чтения: опознанные служебные строки получают подпись («Мm020:» → «Итого:», «Вге20 профиия» → «Всего профиля», «„Итого“» → «Итого»; маркеры «итог», «всего металла»); «Всего профиля» узнаётся по геометрии (ячейка на три графы в строке с массами) — иначе строка «ый» удваивала суммы; скобка перед трёхзначной маркой — «С», «[ОСТ»/«ГОСГ» — «ГОСТ»; цифры марки Tesseract берёт у RapidOCR («С215-4» → «С245-4»); по виду группы: в двутаврах «3061» → «30Б1», в уголках «1140х90х8» → «140х90х8» (только если полка с единицей не существует в сортаменте)
- итог file-14: spec_main 46×10, все массы и обозначения верны, 77 арифметических проверок из 79, два расхождения — ошибки самого чертежа (в «Общей массе» пропущены 0,43 и 0,85), спорных 25 вместо 39. Регрессия на восьми листах без ухудшений: Estakada generic → spec_main 96/97, file-4 и АП КМ1-2 generic → spec_main 60/60 (спорных 12 вместо 33), file-13 generic → spec_main 28/30, file-3/37/45 без изменений, file-7 те же 116/118. Тесты: 155 passed (новый `tests/test_cursive_raster.py`)
- добили остатки: (1) «Фонари» не читались, потому что в кроп повёрнутой ячейки попадали линии её границ и Tesseract молчал — кромки с линовкой срезаются, кроп обводится белым (`_strip_cell_border`), все пять элементов шапки file-14 теперь на месте; (2) «-40» читалось «07-» из-за классификатора поворота RapidOCR, который переворачивал короткое обозначение на 180° — классификатор отключён (`use_cls=False`, текста вверх ногами на листах КМ нет), попутно file-7 стал 120/121 вместо 116/118 и 24 спорных вместо 40; (3) скобка перед словом — это «С» («(Вязи покрытия» → «Связи покрытия», «(редняя» → «Средняя»; настоящие скобки с закрывающей не трогаем), «Связи покрытия» добавлены в словарь элементов; (4) ячейки без роли (штамп): типовые подписи по ГОСТ 21.101 приводятся к канону («Проберил» → «Проверил», «И3М.» → «Изм.», «нодписв» → «Подпись»), смешанное письмо сворачивается в кириллицу («Осmроbсkuu» → «Островскии»); (5) у строки «Всего профиля», опознанной по геометрии, обновляется и normalized_value (в выгрузку уходило сырое «сrееr еее»); «ГОД 27772-2021» — это ГОСТ
- осталось: фамилии и слова в штампе курсивом с систематической подменой в↔б, о↔а («Шаронобва», «Пупон» вместо «Лупан») — без словаря не восстановить, на калькулятор не влияет
- Олег попросил проверить все девять листов из «Новой папки». Прошлись по каждой спецификации глазами: (1) повёрнутые подписи элементов в шапке на векторных листах (Estakada, file-4, file-7) читались шаблонами глифов в кашу «?+ ?ж», «ж ?Ф % К%» — поворот пробовался только для ячеек растрового OCR; теперь и для глифовой каши, а вариант выбирается по «читаемости» (буквы кириллицы минус знаки вопроса), «Связи вертикальные» добавлены в словарь элементов, а короткие подписи сравниваются со словарём по классам похожих букв курсива (б↔в, л↔н, о↔а: «Ванки»/«BaNKU» → «Балки», «Соази бертикольные» → «Связи вертикальные»); (2) марки стали по словарю ГОСТ 27772: «6255» → «С255», «(3556» → «С355Б» (Estakada), латинская «r» в «rОСТ» — ГОСТ; (3) серии профилей по виду группы: «2011» → «20Б1» (file-13), значок двутавра буквой «Т+30Ш2»/«Ш35Ш1» → «30Ш2»/«35Ш1» и битый глиф «3» текстового слоя «+э» → «3» (file-37, «t+э0» → «t30», «Р4+з» → «Р43»), швеллеры «27n» → «27П», «?14П» → «14П»; (4) нечитаемые номера позиций («m», «(-», «О Ж ?Д Ё» на file-7) восстанавливаются по соседним строкам, если нумерация сходится; «D.2» в массах — «0.2»
- итог по девяти листам: file-45 — 236/236 проверок без спорных ячеек; file-4 и АП КМ1-2 — 60/60; file-7 — 121/122 (позиции 1–40 все на месте); file-3 — 49/52 (три расхождения в самом чертеже); file-37 — 136/141 (два блока спецификации на листе, группа труб разорвана между ними — итоги группы сравнить нельзя); file-13 — 28/30; Estakada — 96/97; file-14 — 77/79. Рост числа спорных ячеек на file-37/file-13 — это исправленные и помеченные на проверку обозначения. Урок: два прогона одновременно на 12 ГБ памяти валятся с MemoryError — гонять последовательно
- Олег попросил дожать file-37 и file-13. В ячейке теперь хранится чтение ВТОРОГО движка гибрида (`Cell.alt_text`, есть в JSON), и нормализатор сверяется с ним: серия двутавра «2011» берётся из варианта с буквой, если он есть; наименование группы «Теуеа_КеggраТНаn» на file-13 сопоставляется со словарём по варианту RapidOCR «TpY6Q KBOAPOTHOA» → «Труба квадратная», размер «120х120х6» из наименования сохраняется и переезжает в пустую графу размера. file-37: лишний знак из глифов между серией и номером («20Кз1»→«20К1», «Ш35Кх1»→«35К1»), «?++0П»→«30П», «КхР70»→«КР70»; годы известных ГОСТов правятся по словарю редакций («27772-20861»→«27772-2021»); в уголках добавлены полки 120/130/150 и равные полки не режутся (был испорчен «L150х150х10»); проверка «Итого» группы, начатой в другом блоке листа (наименование — обрывок с маленькой буквы), помечается пропущенной, а не проваленной
- итог: file-37 — все 51 обозначение верны, 136 проверок сходятся, 2 расхождения в чертеже, 3 пропущены; file-13 — обозначения верны, кроме «2011» (I20Ш1: ни RapidOCR, ни Tesseract не видят букву серии — ячейка помечена с вариантами 20Б1/20Ш1), 28/30 (два расхождения — округления в самом чертеже: 8,92+10,12 = 19,04, а «Всего» даёт 8,9 и 12,3)
- осталось: серия двутавра при чтении «1» вместо буквы (нужен эталон масс или словарь сортамента, чтобы выбрать Б/Ш по массе строки)
- Олег спросил про file-45 («читается без проблем?»). Арифметика сходилась вся, но в обозначениях из текстового слоя значок типа профиля стоял хвостом («25Б1 т», «25К1 L», «45М 7», «25х3 □», «8П С») — снимается, если остаток — обозначение (`strip_trailing_glyph`); и во втором блоке не выделялась разбивка по маркам: первая строка «В том числе по маркам» объединённой ячейки чуть заходила на линовку сверху и терялась строгой проверкой «внутри» — строка текстового слоя теперь принадлежит ячейке и при перекрытии > 60%. Итог: 237/237 проверок, обе спецификации без спорных ячеек, таблица по маркам выделена
- Олег: «почему в file-45 не вычисляется удельная масса?» — в форме калькулятора 46 из 120 строк были «Нужен ввод массы». Три причины: (1) в контейнере значок двутавра из текстового слоя приходит не «т», а «1» («25Б1 1») — хвостовые «1»/«I» после полного обозначения теперь тоже снимаются; (2) гнутые трубы «Гн 100х5» не находились в справочнике из-за префикса «Гн» — фронтенд (`cleanMark`) его снимает; (3) для круга «RD18» (EN 10060) в справочнике проката записи нет — масса 1 м считается по диаметру (`roundBarMassPerMeter`, 2,0 кг/м). Итог: все 120 строк с массой, одна («RD18», по формуле) помечена «Требует проверки»
- Олег: «почему не для всех двутавров посчиталась длина?» — в его окне был результат задания, распознанного ДО пересборки контейнера (марки «25Б1 1»); длина считается из массы и удельной массы, а массы у таких марок не было. Чтобы не зависеть от версии OCR, снятие хвостового значка продублировано во фронтенде (`cleanMark`): на старом задании все 120 строк теперь с массой и длиной. Docker Desktop снова зависал на сборке — перезапуск помог

## 2026-09-07
- имена элементов конструкций в шапке блока масс приходили из растрового OCR как есть: «ΦepMbl», «5αAKU», «nodcmponunbuble фермы», «Траберсы» — так они и уходили в калькулятор ОЗМ в колонку «Элемент конструкции»
- завели словарь типовых подписей (`ELEMENT_CANON` в structure.py: фермы, балки, колонны, связи, распорки, прогоны, траверсы, надколонники, рамы, профлист…) и подгоняем к нему через свёртку чертёжного курсива, порог 0.75; вне словаря каша из латиницы/греческого тоже сворачивается в кириллицу, а нормальные незнакомые слова («Косоуры») не трогаем. Текст ячейки шапки (`title`) не меняется, меняется только `element`
- если два столбца сошлись к одному словарному имени, обоим возвращаются сырые подписи: имя элемента — ключ масс, слияние потеряло бы столбец
- двухстрочные повёрнутые подписи собирались в обратном порядке («распорки Связи,», «базы Колонны, колонн»): после поворота кропа слова сортировались только по x, а короткая первая строка отцентрована и начинается правее второй. Теперь слова группируются построчно
- Estakada: все пять элементов верны («Колонны, базы колонн», «Балки», «Фермы», «Связи, распорки», «Траверсы»), 96 арифметических проверок из 97 (единственный провал — таблица по маркам, куда попала строка наплавленного металла); file-7: «Фермы подстропильные», «Балки», «Надколонники», «Связи, распорки», «Рамы под люки дымоудаления», «Балочные клетки»
- ГОСТ профиля у прямоугольных труб на Estakada брался из марки стали (27772-2021): OCR потерял слово «ГОСТ» в ячейке наименования («…прямоцеольные
30245-2003»), номер не распознался как стандарт и пропал при приведении названия к каноническому, а импорт ОЗМ подставлял ГОСТ стали запасным вариантом. Теперь в ячейке наименования профиля голый номер вида «30245-2003» считается ГОСТом (`BARE_STD_RE`, только для этой колонки), а ОЗМ берёт ГОСТ профиля только из `profile_standards` и оставляет пусто, если его нет. Estakada: все четыре группы с своим ГОСТом (57837-2017, 30245-2003, 8510-86, 19903-2015)
- марка двутавра «25Ш0» читалась как «25ШО» («I25WO» -> снят значок -> латиница в кириллицу -> буква О осталась), и в справочнике масс ОЗМ её не находили. В колонке размера профиля буква О/O после буквы серии (Б/Ш/К/Д/М) теперь становится нулём (`fix_series_zero`, только для растровых источников, с пометкой в notes); на стороне ОЗМ `handbookKey` делает то же защитно. Estakada: 27 из 27 профильных строк с массой
- OCR и ОЗМ сведены в один проект: папка переехала в `ozm-new/ocr`, появился Dockerfile (python:3.12-slim + tesseract-ocr + rapidocr-onnxruntime, tessdata из репозитория) и сервис `ocr` в `dev/docker-compose.local.yml`; nginx фронтенда проксирует `/ocr/` на контейнер вместо `host.docker.internal`. Чертёжные шрифты (ISOCPEUR, GOST) в образ не кладём — монтируем `C:/Windows/Fonts`, каталог задаётся `OCR_FONTS_DIR`; в `FONT_DIRS` добавлен `/usr/share/fonts/windows`
- подготовлен вариант развёртывания на сервере с постоянной ссылкой: `dev/docker-compose.yml` (пути через `../`, чтобы работать без junction-ов), `dev/deploy/` (шаблон nginx с паролем и проверкой API-токена, Caddyfile для HTTPS, пример .env), `dev/scripts/deploy.sh`, описание в `docs/deploy.md`. Бэкенд остаётся в dev-режиме аутентификации (OIDC не реализован), поэтому наружу публикуется только nginx с basic auth; токен для /api зашивается в бандл при сборке (`VITE_AUTH_DEV_TOKEN`), интерцептор Bearer перенесён в общий `utils/api.ts`. Самого сервера пока нет — ждём адрес/доступ
- через `curl` без поля `ocr` сервер берёт `ocr=none` и лист без текстового слоя целиком уходит в `unreadable` — это не регрессия, веб-интерфейс по умолчанию шлёт `auto`; при ручной проверке API передавать `-F ocr=auto`

## 2026-09-04
- Олег на `file-7 (339).pdf` показал, что схематические значки сечения в колонке обозначений (□, Ι, L, [) распознаются как символы: «口200х10», «I 35К1», а «I» у цифры превращалась в «1» («135К1»). Значки теперь снимаются до остальных правок и пишутся в notes; «+40» читается как t40; одинокое число в листовой группе («116», «10») — толщина t16/t10; двойной разделитель «46.,9» схлопывается
- на том же листе не определялись виды профиля в калькуляторе ОЗМ: наименования групп курсивом ISOCPEUR гибридный OCR читает в кашу («nроkаm 20рАНеКОmаНblU»). Для сопоставления со словарём групп и маркерами служебных строк добавлена свёртка типичных подмен этого шрифта (n→п, m→т, 2→г, bl→ы, Φ→ф…) — только для матчинга, текст ячейки при совпадении приводится к канону. Словарь пополнен («Прокат горячекатаный», «Сталь листовая горячекатаная», гнутые листовые профили). «Итого» в виде «Vmс20:»/«Мm020:» и «Всего…» узнаются нечётко. Шаблоны глифов всегда включают шрифты, объявленные в PDF
- роли колонок при частично прочитанной шапке: «№ п.п.» и «Общая масса» назначаются по положению относительно блока масс; блок масс по элементам находится по обрывку «по элементам» или по положению между «№ п.п.» и колонкой с «масс» (лист «Эстакада»: раньше таблица была generic без масс, теперь spec_main, 96/96 проверок). «Всего профиля» сверяется с суммой «Итого» плюс строки без своего «Итого»; допуск масштабируется под точность чисел листа (одна десятая — 0,2 базовых, 0,06 на слагаемое). file-7: проверок 116/118 вместо 40/50, спорных 42 вместо 100
- «35W2» → «35Ш2»: латинская W в карте двойников
- ГОСТ извлекается и с точкой/переносом после «ГОСТ», и с латинскими двойниками («FОСТ 19903-2015», «ГOCT P 57837») — в каноническом виде; в ответе веба ГОСТ сортамента и ГОСТ стали отдаются раздельно (`profile_standards`, `steel_standards`), калькулятор ОЗМ показывает в колонке «ГОСТ профиля» первый
- «ОСТ 8510-86» с 4–5-значным номером — это ГОСТ без «Г»; «В том числе nо марком:» узнаётся как заголовок разбивки по маркам (иначе её строки «С255»/«С345» попадали в спецификацию как данные)
- кнопка «Остановить» рядом с индикатором обработки: `POST /api/job/<id>/cancel` выставляет флаг, конвейер проверяет его перед каждой страницей, блоком, вызовом OCR и каждые 40 ячеек и бросает `Cancelled`; статус задания — cancelling, затем cancelled (200), страница показывает «Распознавание остановлено». Самый долгий неделимый шаг — один вызов растрового OCR на блок, поэтому остановка занимает до полуминуты
- урок: heredoc в инструменте Bash съедает уровень экранирования (\n → перенос строки, \b → backspace) — патчи с обратными слэшами писать файлом
## 2026-09-03
- Олег на `file-7 (339).pdf` показал, что схематические значки сечения перед обозначением (□, Ι, L, [) распознаются как символы: значок двутавра становился «I» или «1» и склеивался с номером («135К1»), квадрат — «口». Решили значки не учитывать вовсе: в колонке размеров они снимаются до остальных правок (в текстовом слое — только настоящие символы □/∟/⌶), факт пишется в notes. Там же: «+40» в колонке размеров — это чертёжное «t40» (перекладина посередине читается как плюс); одинокое число в листовой группе («116», «10») восстанавливается как толщина t16/t10 с пометкой на проверку — группа считается листовой по слову «лист» или когда большинство обозначений в ней уже «tNN» (наименование курсивом OCR читает в кашу). Двойной знак умножения «140×x10» схлопывается. Тесты: 135 passed
- Олег: на file-7 в калькуляторе не определились виды конструкций — наименования групп («Двутавры стальные…», «Швеллеры…») приходили кашей чертёжного курсива («Аbуmаbрbl сМα/bНblе»). Сделано: (1) шаблоны глифов теперь всегда включают шрифты, объявленные в самом PDF (ISOCPEUR/Italic), а не только угаданные по метрике; (2) для сопоставления со словарём групп и маркерами служебных строк текст предварительно сворачивается картой устойчивых подмен курсива (n→п, m→т, 2→г, bl→ы, Φ→ф…), ГОСТ/ТУ из сравнения убираются — все шесть групп листа приводятся к канону, «Всего профиля»/«Масса металла»/«В том числе по маркам» узнаются нечётко, «Итого» в виде «Vmс20:»/«Мm020:» — отдельным правилом; (3) когда шапка прочиталась частично, «№ п.п.» и «Общая масса» назначаются по положению относительно блока масс по элементам — без этого таблица переставала быть спецификацией; (4) «Всего профиля» сверяется с «Итого» подгрупп ПЛЮС строками без своего «Итого»; (5) допуск арифметики масштабируется под точность чисел листа (массы в одну десятую → 0,2/0,06 вместо 0,02/0,006), иначе честные округления краснели; (6) «46.,9» → «46,9». Итог по file-7: проверок 116 из 118 вместо 40 из 50, на проверку 42 ячейки вместо 100; file-3 и KR-37 без регрессий. Тесты: 138 passed
- замечено, но не сделано: наименования групп курсивным ISOCPEUR на этом листе читаются гибридом очень плохо («nроkаm 20рАНеКОmаНblU»), а весь блок ушёл в OCR из-за спорной шапки; векторное чтение с шаблонами isocpeui для таких листов стоит проверить отдельно
- интеграция с калькулятором ОЗМ (`ozm-new`): страница умеет жить во фрейме калькулятора. Режим `?embed=1` (или открытие из другого окна) добавляет кнопку «Передать в калькулятор огнезащиты» — результат уходит `postMessage`'ом `{type:"ocrpdf:result", tables}` родительскому окну только по нажатию; во фрейме страница светлая. Префикс `/ocr` теперь берётся и из заголовка `X-Forwarded-Prefix` от nginx калькулятора — сервер по-прежнему живёт в корне на :8000 и работает обоими способами. На ответы добавлен `Access-Control-Allow-Origin`
- Олег показал на `file-3 (487).pdf` (лист с /Rotate 270, чертёжный шрифт ISOCPEUR), что OCR путает значения и теряет числа: «22.010» -> «22010», «0.145» -> «0.14 5», «Итого» -> «Мm020». Разобрались: у листа исправный текстовый слой со всеми числами, но строки текста брались в координатах mediabox, а сетка ячеек — в повёрнутых; ни одна строка не попадала в ячейку, и вся таблица уходила в растровый OCR. Теперь текст поворачивается вместе с линовкой. На этом листе 192 из 201 ячеек берутся из текстового слоя, спорных 15 вместо 35, спецификация опознана и 49 из 52 арифметических проверок сходятся (три оставшихся — ошибка в самом чертеже: «Итого» уголков 1,563 без слагаемого 0,188). Время 24 с вместо 116
- `PIR3017-26-KM1_v6-20.pdf` вообще не разбирался: MemoryError. Кластеры высоких элементов чертежа принимались за буквы, под них рендерились шаблоны 2000×2000 px на каждый символ, кэш из 512 наборов съедал память. Кегль шаблонов ограничен 240 px, кластеры выше 170 px не калибруются, кэш 160. Лист теперь читается за 22 с, 184 проверки из 184
- «шапка спорная -> сразу OCR»: результат OCR брался без сравнения, и ячейки, где OCR не увидел слова, оставались пустыми при живых глифах. Теперь если OCR тоже спорен, блок читается глифами и берётся лучший вариант; ячейка без слов OCR дочитывается глифами
- пробник шапки читает первые пять строк «текстом»; когда шапка короче, строки данных оставались с буквами вместо цифр («0,В2», «tВ» на KR-37). Такие ячейки перечитываются алфавитом роли. В чисто числовых колонках (массы, № п.п.) буквы-двойники переводятся в цифры (В->8, З->3, О->0, Ч->4, б->6) с пометкой на проверку; в текстовых колонках карта прежняя, «В25» не трогаем. На KR-37 спорных 117 -> 105, расхождений 6 -> 4, оставшиеся — ошибка чертежа (Итого 4,63 при 0,63) и один глиф «2» с низкой уверенностью
- нормализация чисел: пробел внутри дробной части склеивается («0.14 5» -> 0.145, с пометкой), полноширинные цифры RapidOCR («５») приводятся к обычным; в плоский лист Excel вместо пустой ячейки теперь попадает распознанный текст, если число не разобралось
- на скане «Спецификация металла образец» гибрид брал RapidOCR «'0» вместо Tesseract «0,34»: если Tesseract дал число с десятичным знаком, а у RapidOCR цифр меньше — берём Tesseract
- одиночная греческая буква («δ=4 мм») больше не считается битой кодировкой ни в ячейке, ни на уровне страницы
- окружение: venv проекта переехал на `D:\dev\venvs\ocrpdf`, Tesseract — в `D:\dev\Tesseract-OCR` (путь добавлен в `TESSERACT_PATHS`); на C места почти нет. Тесты: 132 passed

## 2026-08-20
- на проде https://calclab.pro/ocr выгрузка в Excel падала: OCR подсунул в ячейку управляющий символ (`IllegalCharacterError`, «Зеп halia масса, т»). Такие символы теперь вычищаются при записи xlsx, весь прогон из-за одной ячейки больше не валится
- веб-интерфейс умеет жить под префиксом `--base-path /ocr` (для скрытой страницы calclab.pro/ocr): API и ссылки на файлы идут относительно префикса, без него поведение как раньше
- перезапустили локальный веб-интерфейс на http://127.0.0.1:8000/, чтобы подхватил ускорение OCR; старые процессы на 8000 и 7860 сняли
- программа стала сильно дольше после гибридного OCR: на file-4 уходило ~7 минут. Читали глифами все 360 ячеек чертёжного ISOCPEUR (каша, всё равно выбрасывали), потом ещё раз гибридом, плюс штамп слева и поворот каждой высокой ячейки полным RapidOCR+Tesseract. Теперь: если шапка уже спорная — сразу OCR, тело глифами не трогаем; узкий штамп не кормим OCR; поворот только Tesseract и только если кириллицы ещё нет; модели RapidOCR грузятся один раз на процесс, а не на каждую страницу и не при открытии веба. Тот же file-4: 93 с вместо 416, спека 40×9 на месте, арифметика 47/12 как раньше
- на `file-4 (339.2)` шапка и колонка «Наименование профиля» читались кашей ISOCPEUR («Науменование», «Мроdunu стцльные еНУМbIЕ…»). Подписи колонок и типовые группы профиля теперь подтягиваются к закрытому словарю, если скелет достаточно близок; ГОСТ из ячейки остаётся. «Фахверк»/«Связи» не дотягивали до порога поворота 1,5 — крутим однострочные высокие ячейки с 1,2, merged-наименования не трогаем
- служебная строка номеров колонок на том же листе не попадала в шапку (probe без span), «1 2 3 4  6» уходили в данные. Шапка пересчитывается по полной сетке
- лист «на доработку file-3 (487)»: спецификация стоит рядом с чертежом, и OCR принимал осевые/размерные сетки (105×46, 3×28) за таблицу. Такие блоки больше не читаем — остаётся спека 25×9. Её признали спецификацией металлопроката (шапка «Наименование профиля» / «Фермы», 37 арифметических проверок сошлись); «Общая масса, т» OCR дал как «масса, Ш» — колонку после масс по элементам считаем итоговой

## 2026-08-19
- лист Алексея `file-4 (339.2).pdf` (КМ1-2, «Элементы связей. Фахверк») распознавался криво: в PDF стоит `/Rotate 270`, а `get_drawings()` отдаёт координаты до поворота. Сетка вставала боком (12×42 вместо ~40×9), ячейки кропались не оттуда. Линовку и глифы теперь переводим в систему `page.rect`
- на том же листе вертикаль между «наименованием» и «маркой» рвётся на строках «Итого» — колонки слипались; считаем длину разделителя по сумме кусков. Шапка с латинскими двойниками OCR («Macca», «Науменование») теперь опознаётся как спецификация, а строка номеров колонок «1 2 3 4  6» больше не попадает в суммы как 6 тонн
- после правки лист разбирается как спецификация 40×9: марки С255 отдельно, массы 0,1 / 10,8 / 14,5 / 18,4 / 24,2 на месте; названия профилей по-прежнему плывут — это чертёжный ISOCPEUR, не поворот

- коллега на скрине указал «23857,19» → «23857 49»: RapidOCR принял десятичную запятую за пробел и ещё перепутал 1 с 4, а гибрид взял его, потому что «23857 49» выглядело как число. Если Tesseract сохранил запятую — берём его; если не сохранил — пробел перед 1–2 цифрами сами ставим запятой и красим ячейку на проверку
- localhost:8000 не открывался: порт занял Docker, OCR-сервер после перезапуска туда не сел. Подняли интерфейс на http://127.0.0.1:7860/, туннель pinggy тоже перевели на 7860
- в режиме «Страницы: все» из двухлистовой спеки в результат попадала только вторая страница: номер таблицы сбрасывался на каждом листе, и `table_1.json` со стр. 2 затирал стр. 1. Теперь номера сквозные, во вкладках видно «стр. N», обе страницы остаются в выгрузке
- коллега снова увидел «соединение оборвалось»: короткий POST прошёл, счёт на диске закончился, а финальный GET большого JSON pinggy рвёт. Страница теперь переживает до 30 обрывов, результат жмётся gzip, после Ctrl+F5 готовые задания видны списком — PDF заново грузить не нужно
- «Failed to fetch» на pinggy: туннель рвал POST, пока страница считалась 20–60 с. Теперь файл принимается сразу (202), результат добирается короткими опросами `/api/job/…` — длинное соединение больше не держим
- перезапустили локальный сервер на 8000, чтобы веб подхватил правки сетки и нормализации; туннель pinggy тот же (`current-url.txt` у сторожа), ссылка живёт до конца часа
- скрин из веба с «ВаnКИ / СВ9ЗИ / poroHbl» и кашей в одной жёлтой ячейке — это старый прогон на 6 колонках: внутренние вертикали таблицы-скана выкидывались вместе с рамкой листа. Текущий код на том же PDF собирает 10 колонок, шапка «Балки / Связи / Прогоны» читается с поворотом, арифметика 46 из 47
- «26Б1» оба движка по-прежнему читают как «2651»/«2661» даже на отдельном кропе — букву не угадываем, но теперь такое обозначение из одних цифр жёлтое, с краёв снимается мусор линовки («|2651»), а латинские двойники в марке и размере становятся кириллицей («C255» → «С255», «Tp 160x5» → «Тр 160х5»)
- на листе-скане «Техническая спецификация металла» из 10 колонок распознавалось 6: марка стали, номер профиля, № п.п. и «Колонны/Стойки» слипались в одну ячейку вместе с четырьмя строками сразу
- причина не в OCR: рамку листа мы отбрасывали по одной длине линии, а таблица занимает почти весь лист — её собственные вертикали (778 pt при высоте листа 842) уходили в мусор вместе с рамкой. Теперь длина проверяется вместе с положением: рамка идёт вдоль края листа, разделитель внутри таблицы — нет
- после этого лист разбирается как спецификация (роли колонок, три логические таблицы), а не как безымянный блок: 46 арифметических проверок из 47 сходятся
- строки «Итого» распознавались как строки данных из-за мусора слева («|Итого:») и латинских двойников («B том числе») — их значения попадали в суммы и красили верные числа расхождением; сопоставление с маркером теперь идёт по очищенному ключу
- общий итог сверялся только со строками «Всего профиля», которых на этом листе нет; если их нет — складываем «Итого» по группам, а когда складывать нечего, проверка честно помечается пропущенной, а не проваленной
- подписи «Колонны/Стойки», «Балки», «Связи», «Прогоны» набраны снизу вверх и читались как «KOnOHHbI CTOMKN» / «poroHbl»: узкие высокие ячейки перечитываем на кропе, повёрнутом на 90°, и берём вариант с кириллицей
- осталось нерешённым: обозначения двутавров «26Б1/23Б1/30Б1/40Б1» читаются как «2651/2361/3061/4061» — букву Б на этом скане не берут ни Tesseract, ни RapidOCR, а исправлять по словарю обозначений нельзя, и ячейка при этом не помечается спорной
- на листе КМ без текстового слоя (`2022_08_KM_l_15_19...`) RapidOCR давал верные числа и полную латинскую кашу вместо русских слов: у базовой модели PP-OCRv4 кириллицы нет в словаре в принципе, подкрутить её нельзя
- Tesseract с `rus` (бинарь уже стоит, `rus.traineddata` лежит в `tessdata/`) читает слова верно, но теряет строки и запятые в массах — по отдельности ни один движок лист не берёт
- сделали режим `--ocr hybrid`: строки ищет детектор RapidOCR, читает их Tesseract; выяснилось, что сам Tesseract половину строк на кропе таблицы не находит вовсе («ИТОГО:» пропускает при любом psm, а отдельным кропом читает без ошибок)
- чтобы не платить за это временем, все найденные строки склеиваем в одну картинку с белыми зазорами и распознаём одним вызовом: 0,9 с вместо 12,6 с на сотню строк
- чей вариант брать в ячейке, решает роль колонки; роли на этом листе почти все `unknown` (шапка сама искажена OCR), поэтому для них выбор идёт по содержимому — считаем буквы в варианте RapidOCR
- на листе стало 116 непустых ячеек и 21 спорная против 116/5 у RapidOCR (без единого русского слова) и 66/43 у Tesseract; таблицы перестали помечаться `unreadable`
- режим виден в CLI (`--ocr hybrid`) и в веб-интерфейсе; `--ocr auto` берёт его, когда доступны оба движка
- бесплатный pinggy честный час держит: сессия #1 прожила 3602 с и закрылась по лимиту («Time exceeded»), сторож сам поднял сессию #2 — вопрос закрыт
- перезапустили локальный сервер на 8000, чтобы веб-интерфейс подхватил гибридный OCR
- сервер нельзя поднимать из фоновой задачи агента: она забирает процесс с собой при завершении, и веб-интерфейс тихо умер посреди часа. Теперь стартуем полностью отвязанным процессом через `Win32_Process.Create` — переживает и конец сессии агента
- сессия #2 тоже прожила ровно 3602 с; адрес туннеля меняется каждый час, единственный достоверный источник — `current-url.txt` у сторожа, ссылки из переписки протухают
- завели WORKLOG.md, дальше пишем сюда; прежние сессии в журнал не переносили — истории не сохранилось
- подняли веб-интерфейс локально на 8000 (`./.venv/Scripts/python.exe webapp.py --port 8000`) и выставили наружу, чтобы коллега потестил распознавание со своего компа
- наружу выставляем только туннелем: cloudflared рвётся с edge каждые ~30 секунд и отдаёт 520/530, localhost.run и serveo не проходят ни на 22, ни на 443 — доходит один pinggy по SSH через 443
- проброс порта на роутере отпал не из-за настроек: у провайдера (POWERNET, Волгоград) адрес 213.234.29.205 общий, второй хоп трассировки — приватный 172.30.205.81, входящие до роутера просто не доходят; правило брандмауэра на 8000 так и не понадобилось
- вместо платного Pinggy Pro сделали сторож: держит SSH-туннель, после часового лимита сам поднимает новую сессию и кладёт свежий адрес в `current-url.txt`, история подъёмов — в `pinggy/history.log`
- сторож перестал добивать pinggy: от серии быстрых переподключений сервис придержал наш IP и SSH стал рваться на обмене ключами; теперь ждём адрес столько, сколько живёт соединение, а после короткой сессии пауза с удвоением 15 → 30 → 60 → 240 секунд
- у туннеля два имени сразу (`*.run.pinggy-free.link` и `*.free.pinggy.net`); в файл пишем только первое, чтобы не путаться, какое отдавать
- договорились, что раз в час ссылка меняется и старая умирает сразу: незавершённая загрузка PDF у коллеги оборвётся, готовые результаты лежат в `%TEMP%\ocrpdf-jobs` и живут 6 часов
- пароля у веб-интерфейса нет — пока ссылка жива, грузить PDF может кто угодно, кому она попала
