# Junctions — не коммитить (локальные ссылки на ../backend, ../frontend, ../ocr)
backend/
frontend/
ocr/

# Деплой: пароли и шрифты (лицензии) не коммитить
deploy/htpasswd
deploy/fonts/*.ttf
deploy/fonts/*.TTF

# Env / local
.env
.env.*
!.env.example

# Claude / agent runtime
.claude/
tasks/
progress.md
docs/claude/
tmp/

# OS / editors
.DS_Store
.vscode/
*.code-workspace
*.log
*.exe
.env.share
deploy/share-url.txt
deploy/share-url.log
deploy/share-tunnel.pid
